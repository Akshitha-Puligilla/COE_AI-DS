1)Identify which salesperson has generated the highest total sales
--->select s.sname from salespeoples as s,customers as c,orders as o where s.snum=c.snum and c.cnum=o.cnum and o.amt=(select max(o.amt) as hightest_sales from orders as o);



2)count how many customers are handled by each salesperson
-->select s.snum,count(c.cnum) from customers c,salespeoples s where c.snum=s.snum group by Find out which customers have placed the most orders.s.snum;


3)Find out which customers have placed the most orders
--->select c.cnum,c.cname,count(o.onum) from customers c,orders o where o.cnum=c.cnum group by c.cnum,c.cname  order by count(o.onum) desc;


4)Analyze the average size of orders for each salesperson's customers
-->select s.sname,avg(o.amt) from customers c,salespeoples s,orders o where s.snum=c.snum and c.cnum=o.cnum group by s.snum;


5) Identify the customers who have spent the most money on orders
--->select c.cname,c.cnum,o.amt from customers c,orders o where o.cnum=c.cnum and o.amt=(select max(o.amt) from orders o);


6) Analyze which cities generate the highest total sales
--->select s.city from customers c,orders o where o.cnum=c.cnum and o.amt=(select max(o.amt) from orders o);


7)Fine out which salesperson has the highest average customer rating
--->select s.snum,avg(c.rating) from salespeoples s,customers c where s.snum=c.snum order by s.snum desc limit 1;


8)Calculate the percentage of customers who have placed orders for each salesperson.
---->select sname,(select count(*) from orders where cnum=customer.cnum)/(select count(*) from customers where snum=salespersons.snum)*100 as order_percentage from salespersons,customers where salespersons.snum=customers.snum;


9) Calculate the total revenue generated by each customer.
--->select s.snum,s.sname,sum(o.amt) from salespeoples s,customers c,orders o where s.snum=c.snum and c.cnum=o.cnum group by s.sname;


10)Identify customers who have not placed any orders.
---->select cname from customer where cnum not in select distinct cnum from orders);


11)show the percentage of total revenue generated by each salespersons
--->select sname,(select sum(o.amt) from orders o,customers c where o.cnum=c.cnum and c.snum=salespersons.snum)/(select sum(amt) from orders)*100 as revenue_percentage from salespersons;